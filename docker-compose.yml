services:
  postgres_db:
    image: postgres:latest
    environment:
      POSTGRES_USER: atmt_admin
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: atmt_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
